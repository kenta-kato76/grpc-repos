package main

import (
	"grpc-repos/controller"
	"log"
	"net"
)

func main() {
	lis, err := net.Listen("tcp", ":8080")
	if err != nil {
		log.Fatalf("failed to listen: %v", err)
	}
	controller.ServerStart(lis)
}
